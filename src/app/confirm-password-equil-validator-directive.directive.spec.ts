import { ConfirmPasswordEquilValidatorDirectiveDirective } from './confirm-password-equil-validator-directive.directive';

describe('ConfirmPasswordEquilValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPasswordEquilValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
